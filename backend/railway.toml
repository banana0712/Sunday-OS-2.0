# Railway 部署配置
# Railway 会自动检测 Dockerfile 并使用它构建

[build]
  # 使用 Dockerfile 构建
  dockerfilePath = "Dockerfile"

[deploy]
  # 健康检查路径
  healthcheckPath = "/health"
  # 重启策略
  restartPolicyType = "always"
